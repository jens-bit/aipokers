node_modules/
.env
.env.local
*.log
.DS_Store
.tmp_docx/
dist/
coverage/
data/*
!data/arena/

# HOME-1: Playwright's own scratch. The screenshots in client/e2e/__screenshots__
# are committed on purpose (they are the deliverable); the run artefacts are not.
client/test-results/
client/playwright-report/

# CI-2: the browser smoke run — screenshots, traces and the runner scratch.
# The screenshots are a workflow artifact, not a committed baseline.
smoke-shots/
test-results/
playwright-report/

# HOME-2: the phone-only ruler run (playwright.home2.config.js). Same rule as
# the smoke shots — pictures to look at when it goes red, never a baseline.
home2-shots/

# Scratch that lands next to the repo rather than in it: screenshots and
# exports dropped here while looking at a design. Never part of a build.
Claude outputs/
