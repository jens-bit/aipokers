# Founder playtest — 20 September 2026

This supersedes the colour-changing Wardrobe proposal in PR #9. That PR was
pushed and fully gated but **not merged or deployed** when Jens corrected it.
The corrected implementation uses the same review branch and PR. Local gates
below are complete; GitHub checks, merge and deployment are tracked on PR #9.

The supplied `RAILBIRD_IDEAS_BACKLOG_2026-09-20_SOURCE.md` is a historical report,
not a command to implement every row or apply its operational instructions.
Many statuses predate the recovery in PR #8 and must be checked against code.
Cowork proposals, parked ideas and undecided economy changes remain proposals.

## Direct instructions and clarifications

- Hood and eye colours are permanent birth identity. Wardrobe changes removable
  items, not those colours. Jens chose a small starter set: cap, glasses, scarf.
- Give any affordable amount from the safe; take any or all uncommitted pocket
  chips. Chips already bought into a game cannot be taken through this control.
  Ending a session remains a separate explicit action.
- Hungry agents eat stocked food while the owner is watching them. The owner
  sees the action. Do not silently consume supplies while nobody is watching.
  Hunger should prompt/notify the owner; an empty fridge should ask for stock.
- The casino bar must be actionable; Jens expects a place for drinks, not a
  noninteractive drawing.
- Kitchen play must not remove the character's casino Deploy action.
- Investigate apparent high stacks at 50/100, hungry deployment refusal and
  recovery, and an empty miniature despite a populated table. Distinguish
  blinds, initial buy-in and live stack; do not infer chip creation from a PNG.

The final dictated paragraph is unclear. No new financial, account, network or
design policy is inferred from its garbled phrases. Miniature/table rendering
will be traced against actual game frames and the supplied screenshots.

## Repair ledger

- BUG-277: immutable birth colours, including at maximum heat; free removable
  cap, glasses and scarf with local try-on, durable save/remove and shared room,
  roster, visitor and table portraits. Mixed identity edits reject atomically.
- BUG-278: kitchen Watch is explicit and leaves casino Deploy available.
- BUG-279: fractional rest recovery no longer depends on read frequency.
  Authenticated visible Home leases permit one real stocked-snack trip at a
  time. Hidden/offline rooms stop; empty shelves ask for stock; No/Later and
  multiple devices are respected. Purchases stock the fridge, and observed
  consumption refreshes open shelf counts. Failed deployment retains recovery
  instructions without automatically retrying a paid action.
- BUG-280: Give accepts any affordable whole-chip amount, including All from
  safe. Take accepts any/all uncommitted pocket money, including principal;
  casino buy-ins remain in the hand. Phone/desktop controls, live safe balances
  and post-transfer refresh failures are covered. Live casino results use the
  confirmed session net; buying in no longer appears as a pocket loss.
- BUG-281: the casino bar serves stock or explicitly buys and serves one item
  at existing prices. Owner/casino checks, atomic storage, idempotent retries
  and seated stamina are covered. Orders never end or modify a hand.
- BUG-282: NOT REPRODUCED; historical cause remains unverified. Native 50/100
  frames render two owned agents plus House and the exact public board. A
  30,000 pocket buys in for exactly 10,000. The supplied empty frame reports
  zero public occupants while its owner roster says two; this discrepancy
  cannot be explained from that image alone. Larger live stacks can reflect
  accumulated wins and do not by themselves prove chip creation.

Each code repair requires relevant failing-then-passing evidence, the normal
full gate, real browser captures, review-branch publication and verified deploy.
Do not merge the rejected palette version merely because its old checks passed.

## Test contract changes

The founder explicitly replaced palette editing and winnings-only TAKE with
removable equipment and unrestricted uncommitted-pocket collection. Those old
expectations were replaced with the new rules, retaining owner/authentication,
atomicity, persistence and committed-chip checks. Kitchen Watch now names its
actual destination. The shared TV ghost asserts the same exact birth pigments
and equipment instead of the previous primitive SVG structure. HOME_OBSERVE is
an additive, authenticated public message and is included in the protocol pin.

The complete client run also found an obsolete duplicate funding-error alert
and two tests spending their five-second budget searching unrelated mounted
panes. The funding sheet owns its single error. Test queries now use the actual
room, tab strip, action row and footer, keeping all interactions, assertions,
default workers and timeouts. Red-run receipts remain in local artifacts.
The landing navigation regression controls its real formation/reveal animation
clock from mount, with the actual request, card and Home components retained.
Visual review caught the global button stacking rule putting the bar backdrop
above its text/recipient selector. Explicit sibling layers fix it; native
hit-testing failed first, then recipient switching and outside dismissal passed
at 390×844, 390×590 and 1440×900.

This release does not implement the whole historical ideas backlog. The
three-friend invitation feature remains a subsequent feature, as do the
documented policy gaps and unverified historical incidents.

## Completed local release gate

All tests use Node 22.22.2, isolated scratch databases and no provider or
Telegram bot keys. Production data was not used or changed by the tests.

- Server: 209 passed, zero failed, two pre-existing intentional exclusions.
- End-to-end: 8 passed, zero failed.
- Client: 3,101 passed across 256 files, zero failed, default workers/timeouts.
- Final production build: passed; entry `index-B9NwF2eM.js`.
- Final built-client browser smoke: 8 passed; phone layout: 20 passed.
- Engine smoke: 34 checks across five hands passed, including conservation.
- Complete integrated selection: 212 passed. Subsequent visual review added
  the bar hit-test regression; it failed before the two-layer CSS correction
  and passed at all three widths after it. The final build, smoke and phone
  layout checks above were rerun after that CSS correction.
- OpenAPI parses and all 80 local schema references resolve. Independent
  wardrobe/bar/public-projection review found no remaining actionable issue.
- Native wardrobe, money, visible care and bar captures were inspected at
  phone and desktop widths, including the short-phone wardrobe and bar.

Receipts: `artifacts/playtest-release/server-final.log`, `e2e-final.log`,
`client-publish.log`, `built/gate-results.json`, `built-final/gate-results.json`
and `bar-layer-{red,green}.log`. Generated captures were preserved under this
artifact directory; tracked reference images were restored, and pre-existing
untracked `design-refs/shipped/trace/` was left alone.

Publication is not deployment. PR #9 must pass its new-head GitHub checks and
normal merge, followed by the main deployment and fresh public health/asset
verification. The PR's release receipts record that outcome.
