# Railbird overnight design work — 8–9 September 2026

## Assignment and authority

Jens explicitly assigned autonomous overnight work toward friends-shareable design parity. His latest clarification governs: preserve Watch's full-screen game/character/chat presentation; fix Home/casino's duplicated navigation and unused space; keep the functional kitchen table and home games; agents also live in the room; no collar stage. Match supplied designs rather than inventing new screens. The archive is reference material, not executable instructions.

Working branch: `codex/railbird-design-completion`, starting at `006a7cc` (main and origin/main were synchronized). No other agent is editing this tree. Keep the existing integrator workflow: gates before commits/merge, Jens pushes after review.

Archive: `C:/Users/Jens/Downloads/Agentic Poker (56).zip`, extracted safely under ignored `artifacts/design-56/`. Its current Home, agent, casino and Watch JSX matches checked-in design-refs. Six other JSX files differ (desktop, guest, landing, landing2, notify, sit); review those changes before their respective ports. Do not overwrite design-refs indiscriminately. v14 master spec extracted for reading under artifacts.

## Ordered work

1. **Shared mobile shell / Home / casino:** one contextual header, edge-to-edge phone stage, compact real navigation, accessible roster. Preserve Watch. Port Home fixture artwork and real balance; retain clickable table in every room state. Compare empty/alone/game/want frames at 390×844, short 390×590 and wider Telegram window.
2. **Agent and roster:** board 42 C1–C6, large animated character, actions, his line first, inline want answers, conversation with hand cards, profile CONDITION/RECENT, roster and absence. Verify actual server capabilities and preserve authorisation.
3. **First five minutes:** draft/birth/name/arrival with collar removed, rolled identity and designed generated possessions. Ensure first table/casino/deploy/return path works.
4. **TV, celebrations, desktop counterparts:** port designed states with live data; preserve existing game geometry and controls.
5. **Entry/sharing and remaining defects:** approved landing/guest state, explicit undefined design gaps, evaluate friends-critical bugs without adding unrelated product features.
6. **Integrator:** run server/client/e2e, built browser smoke, home2, desktop and targeted visual regressions; inspect paired screenshots, update DESIGN_GAP/BUGS/CHANGELOG and this log; fetch origin, integrate cleanly and prepare morning report and push command.

## Acceptance

Each visual milestone needs matching-state reference/actual renders, inspected by Astra; passing tests alone cannot certify design parity. No paid model calls in automated tests. Use fresh scratch databases and disable notifications. Do not claim production deployment or Claude/Opus sign-off. Undesigned features and external rollout settings are reported honestly.

## Progress

- Read CLAUDE.md, HOW_WE_WORK, current design gap, archive README and relevant v14 sections. Archive confirms the room's F05 alone frame is not a complete definition of the home-game states.
- Confirmed global Header stacks above CasinoHead/FloorView header; mobile room scale caps at 1 even in wider Telegram windows. Home furniture is a simplified port (safe lacks balance, fridge lacks stock detail, couch lacks reference anatomy).
- Active goal created. Scheduled continuation every 30 minutes is configured in this chat, stopping by 09:00 Europe/Malta on 9 Sep. Current work is active; no implementation milestone is yet certified.

## Resume here

Batch17 ready to commit and integrate; previous16 caf9083. C9 shared DESK_ROOM560700 drives HomeFlat, AwayWall, HomeGame, routines/seats/newborn door, Carry conversion/drop targets and bubble blockers. Phone defaults preserved. Empty desk now actual room, forming StageGhost150 portal at280318; latest C9 padding47x30 includes DkFlat's extra54 allowance. Normal first-draft test intentionally changes retired Make-an-agent placeholder to real-room invitation. HomeFlat export compatibility fixed after initial transform failure. Final authoritative counts: server105/2skip,client2181/2todo,e2e7pass82.6s,buildpass,36browserpass inclBUG55,4built smoke,20Home2 no skips. Final logs batch17-recovered-test-all-final.log,batch17-recovered-browser.log,batch17-recovered-build.log,batch17-smoke.log,batch17-home2.log. Final pairs17desktop-c9/guest-room1280 refreshed and inspected. At04:56 workers/browsers stopped; at08:53 no matching processes, recovered gates. Initial recovered share test native exit3221226505, isolatedpass+fullrerunpass, BUG94OPENenvironmentcauseunproven. Do not claim uninterrupted overnight progress. Refcrop audit C1 independently verified; c2-c7a rendered, c7b clone lacked rendered innerdiv and timed out, fullauditunfinished. Fetchorigin confirmed006a7cc unchanged,16commitsaheadbefore17. HeartbeatPAUSEDat09. Current scratchserver64904 port18886cwdartifacts/smoke-batch17; stop after integration. Sandboxchangedread-onlyanddefaulttoolsfailSID; require_escalatedexecworks, normalview_imagefails; useSystemDrawinginmemoryJPEGthroughescalatedexecforlocalvisualinspection. No push. Morningreportread-me-claude/MORNING_REPORT.md lists incompletefullscope. Goalremainsactive/unachieved. Preparemainfastforwardafter17commit, exactpushcommandonlythen. Remainingpriority /welcome,C9compactprofile/header,generatedpossessions/brandstates,metadata,crops,externalrollout. Do not equate morning cutoff with goal completion.


Batch16 complete. Previous15 committed c79943e. L2 guest phone full room/no extra header/half-height glass/one vertical sign/empty chair; table retained, real recruiter/four no-collar stages. Desktop guest extra global header hidden; wider room and forming preview still PARTIAL. BUG92 duplicate sign red then repaired; BUG93 exact settled room y was20 phone/4 desktop due competing focus helpers, now GuestLanding owns scroll and preventScroll focus, embedded BirthScreen skips helper, normal drafts scroll only if obscured. Initial missing guestBoot forwarding to AppShell caught by five browser failures and fixed. Full gate initially caught obsolete parseFloat(max()) CSS assertion, intentionally updated to wave61 responsive rule; actual clearance held by browser. Final server105/2skip,client2175/2todo,e2e7pass95s,buildpass,12draft+landingbrowserpass,built smoke4,Home2all20pass no skips. Both reference/actual guest-room pairs inspected; original reference renderer's26px clip was corrected by cloning the full room frame out of canvas. Logs artifacts/batch16-*.log. Fresh built server61192 at18886 cwdartifacts/smoke-batch16, keys/notify off. No push/mainmerge. Next C9 wider560x700 desktop room: parameterize shared geometry for fixtures, seats/routines/newborn doorway, carry mapping and bubble blockers; never stretch art separately from hit/placement coordinates. HomeFlat/AwayWall/HomeGame/useCarry/roomBubbles currently close over phone constants. Latest reference mood-desk59.jsx DkFlat/DkFixture/dkRoom: table280400rx128ry84,safe24967862,fridge45213072112,tv21060414078,couch22470112148,door51828842132,frames24167662gap10; fit max1.4 with30pxside/20pxverticalmargin. Keep Watch presentation. Remaining /welcome/C9profile/header/metadata/external rollout and finalintegrator. Goal/heartbeatactive; time~04:39Malta,deadline09:00.

Batch15 complete, ready to commit; batch14 committedf2ca393. BUG45 isolated original-t command reproduced missingpocketcopy; heading rendersbeforeasyncrooms+selectedRoomeffect, awaitsameexactfindByTextpassesalone. No sharedstateneeded/productchange. BUG43 unfixmedpreservedbody failedfreshUIDbug43fresh15 expecteddoor:born gotfloor:0. Waitfornotdoor:born anddata-walkingfalseBEFOREstartingmeasurement; originalsame-spot/noPOSTassertsunchanged. IsolatednewUIDbug43settled15passes, fullfreshUIDbatch15fullHome2all20passNOSKIP. Testonlybatch,no clientbuildneededbeyondverified14. Fulltestallserver105/2skip,client160files2173/2todo,e2e7pass121s. Logsartifacts/batch15-*.log. Existingserver8679018886scratchbatch14stilllive,no keys/notify. Newrefsartifacts/reference-guest-room-{390,1280}.png generatedfromunpackedL2canvasusingrender-landing-ref.cjsclipbelowhero; inspectnext. Next16: first-agentroomvisuals duplicatehorizontaldoorTagplusverticalCASINO, latestL2RoomPhonehalfheightglass +emptychair, guestroomnoextraheader, desktopguestroomrightcolumnworksbutoldtopbar. Considerkeyboardshortviewportbeforechangingglobaldraftsheet206top. No mainmerge/push;goalsactive;time~04:15Malta.

Batch14 complete, ready to commit. Previous13 committed42149c9. BUG89 DesktopHome ignored App's onReplay; now local owner-authorized resolveDeepLink callback feeds its own theatre, expiredhand→companion, Back returns persisted CasinoBoard. New unit initially used wrong-case Casino name corrected before actualred(disabledcallback); savedlookup/header/fallbackthenpass. BUG90 nullrecordedstacks→dash rather than0 inDeskTableStage/SeatChip. BUG91 replaycomplete usesrecordedboard/pot/endlabel andnolivesitout. Redbeforefix, firstnewboardcardselector nonexistent corrected, localeNBSPexpectednowrawtextContentexact, duplicateHANDlabelscoped scrubber. Initial browser expectedFloorafterBack but existingCasinoScreensessionStorage correctlyrestoresBoard; assertBoard+sameownedrow instead. End screenshotwaitstugfilltransitionbeforecapture; inspected client/e2e/shots/overnight-batch14-desktop-replay-end.png. Fulltestall server105/2skip,client160files2173/2todo,e2e7pass108s. Browserdesk+watch32pass(22desk+10watch),built smoke4,Home2 19+existingBUG43skip,buildpass. Logs artifacts/batch14-*.log. Scratch server86790 at18886 cwdartifacts/smoke-batch14, keys/notifications off. Restore only regenerated old __screenshots__,keepnewartifact. No mainmerge/push. Next BUG43 floor-drop gate and BUG45 isolatedCasinoScreen, then remainingvisualwork (mobile firstdraft duplicate horizontal/verticalcasino signs; longwelcome/guestentry; widerC9DkFlat560700). Watch/replay furtherobservedmetadata debt: DeskTableStage eqstripreads lastDecision ratherthansnapshot (canconflictwithrope), replaydraws defaultheat45anddefaultidentity because recordsnotpassedagentmetadata. Do notinventhistoricheat. Current time~04:08Malta, deadline09:00. Goalactive.

Batch13 complete, ready to commit: L2 hero26px preview/phone180 desktop280 character, 54/85px backs measured from actual hood, fists, Inter/Rozha, first desktop recruiter in right rail beside real empty room. BUG87 unit failed99vs54 then passed. BUG88 browser had attached/focusable input hidden in zero-height full-stage content; actual elementFromPoint failed then passed after rail-first empty draft and flex fallback. First-draft CTA hidden while drafting. Initial browser bbox-only assertions missed the defect; keep stronger hit test. The previous empty-desk stage-sheet expectation intentionally superseded by wave61. Full test:all server105/2skip, client159files2169/2todo, e2e7pass91s. Landing+draftbrowser11pass, strengthened BUG88 extra1pass. Buildpass, built smoke4, Home2 19+existingBUG43skip. Pairs landing390/1280 inspected; actual first-desktop draft screenshot saved. Logs artifacts/batch13-*.log. Scratch server93304 at18886 cwdartifacts/smoke-batch13, no keys/notifications. Archive standalone landing unpacked via artifacts/unpack-landing-reference.cjs to artifacts/landing-reference; it is a bundled design canvas, not deployable as-is, correcting older LAND6 claim. Render helper artifacts/render-landing-ref.cjs clones lp390/lh1280 before screenshot. Remaining entry: long /welcome port, publicbrandhostname fallback, sheen, actual configured bot link, mobile draft duplicate older horizontal tag + vertical sign, newer guest room layout; no VPS switch verified/changed. Next friends-critical desktop casino replay callback ignored and replay unknown-stack/next-deal controls; then remaining room/BUG43/45/marketing and finalintegrator. All goals active, no mainmerge/push. Time~03:55Malta, deadline09:00.

Batch11 committed61763cd. Batch12 complete: C9 companion/380px rail + DkRosterRow identity/condition/location/session result. BUG80/81 saved history/refused send red→green; BUG82 hover position:relative broke fixtures, browser red→green; BUG83 profile identity red→green; BUG84 roster lifetime/location red→green; BUG85 actual desktop replay height collapsed/back blocked, three-size C9 flow red→green; BUG86 BodyBars inline span width0, browser red→green after shared display:block fix. AgentView desktop132px/268stage uses real conversation/latest flagged replay; Profile keeps detailed card; explicit Carry works via useCarry enabled on desk but regular mouse binds remain off. New C9 test's first place mock omitted ?userId, corrected route only. Old tab selectors intentionally→Profile action, width arithmetic520→380 per C9, no assertions removed. Initial test:all failed CSS mobile input audit; desktop12.5px scoped to1100media. Final test:all server105/2skip, client159files2168/2todo, e2e7pass95s. After final BodyBars fix, full client2168/2todo and build pass, complete desktop+Watch browser30pass(20desk+10Watch), built smoke4, Home2 19+1BUG43skip. Pair C9 full/column refreshed and inspected; Watch actual condition screenshot inspected. Logs artifacts/batch12-*.log. Scratch server54375 at18886 cwdartifacts/smoke-batch12, keys/notifications off. Ready to commit12; restore only regenerated old __screenshots__, keep new shots. C9 PARTIAL: latest DkFlat560x700 differs from retained shared390x612 room, latest header and compact desktop CONDITION/RECENT remain. Next entry/landing (standalone archive bundler HTML; mood-landing/landing2 differ), remaining BUG43/45 and desktop replay casino callback ignored (App passes onReplay but DesktopHome never forwards it); replay strip also still displays unknown stacks as0 and next-deal/Sit-out on replay, inspect/fix separately. Goal active, no mainmerge/push. Time~03:31Malta, deadline09:00.

Batch10 committed5973fe7. Batch11 complete: C8 hands/100bb bursts/bust name fall, BUG79 split-pot status. The initial full client gate failed the old WATCH-7 hold-pose expectation; updated it explicitly to latest C8 while preserving session-ceremony assertions. The initial C8 reference crop caught adjacent canvas content; renderer now clones actual frames out of the transformed archive, all three corrected pairs inspected. Three phone-size browser cases plus ordinary-win case pass; effect geometry, composer, pointer transparency and reduced motion verified. Final test:all server105/2skip, client157files2161/2todo, e2e7pass71s; final build pass, built smoke4pass, Home2 19+1existingBUG43skip. Logs artifacts/batch11-*.log. Scratch server96759 at18886 cwdartifacts/smoke-batch11, no keys/notifications. Ready to commit11, then C9 desktop. ThreadPanel is the actual old Home rail via DeskHome; useAgentThread loses saved history and ignores non-OK chat responses, unlike repaired mobile AgentThread. Inspect and reproduce before fixing. Need archive desktop-diff check, reference C9 crop, actual pairs. Entry/BUG43/45/integration still pending. Goal active, no push/mainmerge. C8 audio unselected, Watch cards/strip preserved by user priority.

Batch9 committed2b99433. Batch10 complete: HOME-3 server cadence 5mplay/10mbreak (HOME_PLAY_MS/HOME_COOLDOWN_MS), finishes current hand, preserves clock across roster changes. Automatic solo removed under v14; manual Carry/table sync({manual:true}) still plays House or overrides cooldown, worn/study excluded. BUG78 actual child-process timer regression failed then passed (10m production cooldown compressed to60ms only in probe): timer previously stopped when all paused. Existing solo/table-ID/visit tests intentionally invoke manual path; no invariants removed. Full test:all server105/2skip, client2154/2todo, e2e7pass94s. Initial smoke failed old automatic-solo assumption at SIT; seed now uses actual placement API before unchanged Watch/Sit checks, corrected built smoke4pass, Home2 19+1BUG43skip. No client source changes/build needed this batch; uses verified9dist. Scratch server48322 at18886 cwdartifacts/smoke-batch10, no keys/notifications. Ready to commit10. HOME3partial: coordinated new room-day behaviours are still an undesigned wave, existing routines now visible in breaks. Next C8 celebrations (mood-agent2 C8a/b/c) then C9 desktop, landing/entry, remainingBUG43/45 and finalintegration. Allgoalactive, no mainmerge/push. C8 observed: WatchFelt computes heroWon from only result.winners[0]; default server big-pot threshold75bb. Preserve Watch geometry/timing while adding pointer-transparent 1.2s teal/gold bursts and900ms bust name fall, with actual results. Ref frame ids c8a/c8b/c8c. No C8 edits yet.

Batch8 committed44584fc. Batch9 complete: N3/N3b mobile casino band follows carousel/tray recipient, authenticates sends and loads private thread, phone card uses remaining stage, idle ghost keeps saved identity. BUG75 null send lost draft; BUG76 smooth-scroll recipient oscillation; BUG77 idle card default hood. All reproduced before fixes. Initial short-browser error overlay blocked dot; now pointer-transparent. Final browser3 passes including error/retry/recipient/auth and stage geometry; pairN3b inspected with actual quiet public events. Final gates server105/2skip, client156files2154/2todo, e2e7pass97sec; final client same, build pass, built smoke4, Home2 19+1existingBUG43skip. Logs artifacts/batch9-*.log. Scratch server83328 at18886 cwdartifacts/smoke-batch9, logs redirected, no keys/notifications. Ready to commit9. Next HOME-3 server cadence from v14§6.6: old homeGame starts even one alone against House, 40hands then15moff; manual placement calls sync too. Preserve explicit manual table interaction, no paid home decisions or bankroll/attribute effects. Need read full tests/place/home routines before changing. Then C8/C9, entry and finalintegration. Goal active; no push/mainmerge.

Batch 8 complete: C7 live/tape television, actual recorded board/flag/hand number, authenticated review return to Home and desktop replay integration. BUG-74 failed before correction. TV browser3 at390×844/590 and490×844 passed; both C7 pairs inspected, kitchen table retained, authored sample versus real record differences stated. Full test:all server105/2skip, client2150/2todo across155files, e2e7pass (122sec); built smoke4, Home2 19+1 existing BUG-43 skip. Logs artifacts/batch8-*.log. Scratch built server47676 at18886 cwdartifacts/smoke-batch8, output redirectedserver.log, no keys/notifications. Ready to commit batch8 then casino bottom conversation band (N3/N3b), Home rhythm, C8/C9 and entry/integrator. Overall goal still active; no push or main integration yet.

Batch 6 committed `e7e1bda`. Batch 7 complete: C6 absence + Home count/visit subtitle + vertical DoorTap. BUG-72 spare-chair overlap and BUG-73 visiting-frame label/dead tap reproduced then corrected. No reserved chair state invented: use free positions around the actual two/three/four-player arrangement. Vertical sign and door deliberately share the exact footprint from current ref; old flat.test non-overlap/right-minus-six assertions replaced explicitly, clearance and table click assertions kept. Final gates test:all server105/2 skips, client155 files2146/2 todo, e2e7 pass; final client same; built smoke4; Home2 19+1 existing BUG-43 skip; C6 browser3 sizes clean after correcting a new test's wrong table-sheet testid and adding /slots fixture; BUG-55 two sizes passed. Pair C6 inspected, table retained by user override. Scratch server20862 at18886 under artifacts/smoke-batch7, no keys/notifications. Restore only regenerated old BUG-55 screenshot files before commit. Next C7: Home TV already shows live MiniFelt but tapping it routes to casino, and studying overrides live incorrectly; TapeOnTv is three bars rather than actual hand. Then Home day/casino composer/C8/C9/entry and final integration. Goal remains active.

Batch 6 final acceptance: corrected e2e all7 pass (104s), server105 pass/2 skips, client2141 pass/2 todo. Desktop look15 pass; inspected man/watch shots show the existing tiny desktop thread and mismatched room fixture arrangement, so C9 remains material work, not certified. Two desktop Watch tests log unmocked memory GET connection errors; their fixture needs the memory route in the C9 pass. Built smoke4/Home2 19+1skip passed. PairC4 inspected; restoring only regenerated old desk PNGs before commit, keeping the new pair. Ready to commit batch6, then resume C6/C7.

Batch 5 committed `5ba4a45`. Batch 6 implemented: compact mobile C4 profile, detailed card behind His sheet; authenticated whisper returns its saved conversation into AgentThread; first-time attribute explanations remain per viewer. BUG-70 reproduced stale history across agent switch then fixed by keyed detail state. BUG-71 guest e2e timing reproduced after full client pass: production think delays consumed its 30sec/two-hand budget; existing per-test clock dials compressed, assertions unchanged. Server final 105 pass/2 skips; client final 153 files/2141 pass/2 todo; built smoke4 pass; Home2 19 pass/1 existing BUG-43 skip; C4 browser3 sizes pass, initial C5 rerun pass; targeted profile71 pass and final App+overview34 pass. E2e corrected run still in progress, guest already green1.36s; desktop look run finishing. Logs artifacts/batch6-*.log. Scratch server session3155 at18886 cwd artifacts/smoke-batch6. C4 pair inspected, data differences honestly labeled. Next: C6 absence/visits/header roster count, C7 TV state and correct click destination; Home day cadence; casino bottom band; C8/C9/entry and final integration. Do not mark overall goal complete.

Batch 4 committed `11af8c4`. Batch 5 complete and ready to commit: C5 roster/BUG-69, Send to a friend moved to existing profile More. Final gates: test:all server 105 pass/2 skips, client 151 files/2134 pass/2 todo, e2e 7 pass; final client rerun same; built smoke 4 pass; Home2 19 pass/1 existing BUG-43 skip; targeted roster/visit/profile 50 pass; C5 browser passed with actual API fixtures. Pair `client/e2e/shots/overnight-batch5-roster-c5.png` inspected; known-live count differs honestly, wallet footer retained. Logs artifacts/batch5-*.log. Scratch server under artifacts/smoke-batch5 was started for gates at18886; verify process before restarting, tool session output was lost during compaction. Next: C4 compact mobile profile then C6/C7 and remaining queues. Do not mark the overall goal complete.

Batch 3 committed `dffb4db`. Batch 4 ready to commit: BUG-66 immutable identity + BUG-67 draft scrolling/width + BUG-68 duplicate birth ghost. Tests: server 105 pass/2 skips; client final 151 files/2131 pass/2 todo; e2e 7 pass. Built smoke 4 pass, Home2 19 pass/1 existing BUG-43 skip. Draft browser 7 pass plus final single birth visual check. Last change only scopes the birth action CSS over .dr-app button; final client suite and build pass after it. Native Windows abort -1073740791 occurred in last screenshot invocation, left Vite PID22168 on5199; verified command line before stopping that owned test process, clean rerun passed. No assertion retries. Logs artifacts/batch4-*.log. Server session58657 on18886, cwd artifacts/smoke-batch4, keys/notifications disabled.

Batch4 pair inspected: client/e2e/shots/overnight-batch4-birth-card.png. Reference board23 C2 rendered by cloning its frame out of the canvas transform, animations disabled; wrong older board20 nb2 was discarded as evidence. Actual Granite fixture differs from reference Hothead, and prior BIRTH-4 96px face retained rather than restoring the clipping-prone 64px overhang. Do not certify exact pixel parity. Shared identity implementation moved from client/lib to src/shared with re-export; new server identity test includes a real SQLite legacy upgrade and separate-process restart. Existing clients now consume persisted colors, no Watch geometry changes. Next active work: board42 C4 profile/C5 roster, then C6 absence/C7 TV/Home day/casino band and remaining queues. Morning deadline09:00 Malta; keep working and do not mark goal complete.

Batch 2 committed `892498c`. Batch 3 ready to commit: F13 stock shelf + actual Home conversation, BUG-64/65. Full test:all passed (server 103 pass/2 skips, client 151 files/2129 pass/2 todo, e2e 7 pass); final client rerun after visual spacing also passed. Built smoke 4 pass; Home2 19 pass/1 existing BUG-43 skip. Dev Home 19 pass; desk run 14 pass/1 stale GIVE-selector failure, corrected to F13 BUY 6 with confirmed stock fixture; targeted fridge phone+desk then 2 pass. Logs artifacts/batch3-*.log. F13 ref/actual settled images inspected and composed into new pair. Initial screenshot caught entry opacity; browser now waits for opacity 1. Visible pills follow reference density with 44px hit targets. Scratch built server session 88945, port18886, cwd artifacts/smoke-batch3, keys/notifications disabled. Next: birth/identity and C4/C5, casino footer/TV/routines, then remaining queues. Watch unchanged. Restore only regenerated old test PNGs before commit, keep new batch3 pair. Current first-birth issue: Home collision-avoids colors but solo identityOf does not; server should persist the immutable roll and birth reveal must use it.

Batch 1 committed as `8dceae5`. Batch 2 (mobile agent view) implemented, final gates running, then commit. Next: fridge stock dead end, empty Home/casino conversation strips, first meeting/birth, C4/C5 and the remaining queues. BUG-59 failed before implementation (Home heading absent); BUG-60 failed at 100px conversation band, then 80px, then passed at 76px with a circular 26px send control. Four-agent hit tests still pass. RAILBIRD wordmark/count assertions intentionally changed to approved Home heading + accessible Railbird mark. Casino back-chain assertions intentionally changed to Floor/Board toggle and direct Home. Home2's blanket prohibition of dollar signs excluded the explicitly designed safe/TV fixtures; the rest of the room and table still cannot price seats.

### Batch 2 implementation and handoff notes

- AgentView.jsx + agent.css ports board 42 C1–C3 inside the existing AgentThread network controller. Mobile App passes companion; standalone legacy ChatsScreen remains for its existing callers/tests. App removes global header on the agent route, provides actual deploy/watch/carry callbacks, and preserves origin semantics. FundSheet is reused inline; fund result updates pocket. Want stock opens FridgeSheet while preserving the ask; onGiven reads actual want from the authenticated agent detail endpoint rather than treating any snack as an answer.
- useCarry now has programmatic pick for the CARRY button; next pointer gesture performs existing HomeScreen placeAgent. Window click capture suppresses the fixture's ordinary sheet click after placement. Cancel/Escape makes no request. Short 590px screen initially clipped the pill in a 210px stage; removed that unnecessary shrink, browser checks keep the pill inside the stage. Very short keyboard viewport has a 100px character variant.
- BUG-61 tests failed on refused wants and stock-needed responses; BUG-62 tests rejected chat; BUG-63 failed on lost saved chat. Saved history comes from owner-only agent.chatHistory (server retains 12); late recap cannot replace new messages. No new paid calls. Changes to old placeholder/Profile selectors are intentional board-42 navigation, not diluted assertions.
- Six dev-browser checks passed (C1/C2/C3 and 390×844, 390×590, 490×844 actual Carry placement). Reference renderer artifacts/render-agent-refs.cjs rendered C1–C6. Images artifacts/agent-c{1,2,3}.png and reference-c*.png inspected. C3 hand data/order differs as recorded in DESIGN_GAP; do not label exact parity. Pill's sr-only label initially rendered visibly due to Home-scoped CSS; corrected scoped screen-reader-only rule and measured its clipping. Ref hands use SEAT_GRIP, now ported.
- Final full test:all passed: client 149 files/2121 pass/2 todo, server 103 pass/2 skips, e2e 7 pass (109s). Built smoke 4 pass and Home2 19 pass/1 existing BUG-43 skip. Full dev Home suite 18 pass, including six new agent checks, and extra fresh-stamina geometry checks 3 pass. The file filter said desktop.spec.js, but the real file is desk.spec.js, so this batch's dev run covered Home only; built desktop smoke/casino did pass. Logs artifacts/batch2-{test-all,smoke,home2,home-desktop}.log. Build warning includes >500KB main chunk (503KB uncompressed), plus prior ReplayTheatre mixed-import warning.
- Scratch server is now session 38347 at 127.0.0.1:18886, cwd artifacts/smoke-batch2 (server.log there), no keys/notifications. Old session 55639 stopped. Final build and smoke command session 38275. Full testall session 71085.
- Discovered next-batch issues: FridgeSheet has no GET /api/fridge or POST /api/fridge/stock despite server routes; only /give. Identity is client-derived and Home uses roster collision avoidance while single-agent/roster views use idFor fallback — inspect and unify the birth identity rather than allowing a man to change colors between views. Home/table remains server-backed, no collar stage restored.

### Batch 1 verification and observed limits

- `npm run test:all`: server 103 pass / 2 skips, client 146 files / 2107 pass / 2 todo, e2e 7 pass. Initial native Windows abort in verify-cache-headers.js was recorded. Fixed its forced process exit, response draining and child cleanup, and its 4-second startup deadline (existing BUG-39/34). Exceptions now fail instead of silently succeeding. Full suite passed afterward; the wider BUG-34 family remains open.
- Final client suite after casino header adjustments: same 2107 pass. Build passes (existing ReplayTheatre mixed-import warning).
- Built smoke 4 pass. Home2 19 pass / 1 existing BUG-43 skip (fresh owner `home2final`). Home+desktop browser run 27 pass, including 390×590, 390×844 and 490×844.
- Local reference renderer `artifacts/render-refs.cjs` rendered archive frames F01/F05/F10/F11/F07/F17. Pairs `artifacts/pair-{home-empty,home-alone,home-game,casino}.png` honestly label authored vs live data and different routines. Furniture/header improvements verified; these are not a certification of full-state parity.
- Remaining clearly visible: empty Home lacks its bottom composer; casino lacks the designed bottom conversation band; current all-playing home state differs from reference mixed routines; TV still shows room counts rather than the designed biggest-pot/state feed; horizontal door marquee differs from the archive's vertical door label. Keep functional table by founder override. Address these in the upcoming batches rather than relabeling them complete.
- Scratch built server active on 127.0.0.1:18886, session 55639, cwd artifacts/smoke-batch1-final; no keys, notifications off. Stop before new built-client verification and use fresh scratch data. All completed test processes have ended.

## Day continuation — batch 18

Batches 1–17 were fast-forwarded to main at c6130b4, with a clean main test:all gate and fresh origin fetch (origin remained 006a7cc). Nothing was pushed. Scheduled heartbeat was paused at its 09:00 cutoff; the user's active goal continuation authorizes this work to continue on codex/railbird-design-completion.

Compact desktop profile is implemented and its reference pair inspected. BUG-95 was reproduced before repair. Targeted unit checks: 25 passed. Full desktop browser suite: 22 passed; then three C9 cases passed with additional profile whisper retry and funding boundary checks. A new funding-bound assertion was corrected to account for the existing one-pixel column border; no interaction assertion was removed. Full test:all gate is recorded in artifacts/batch18-test-all.log. The initial root-level npx vitest invocation resolved an unrelated cached Vitest 5 and failed before running tests due to missing jsdom; rerunning the installed client Vitest 2.1.9 produced the intended red regression, then green. Next: latest C9 header/navigation, remaining welcome/identity/replay details, and final built-app integration gates.

Batch 18 final gate: server 105 passed / 2 live skips; client 2185 passed / 2 existing todos; e2e 7 passed. Desktop 22 passed plus three extended profile browser cases. Ready to commit; built smoke/Home2 will be refreshed with the next integration build before a push report.

## Day continuation — batch 19

Batch 18 committed bf8b233. C9 header/door navigation implemented and visually paired. Targeted header/Home/casino unit checks 69 passed, followed by the two preserved replay tests. Full browser set: 37 passed (desktop, draft, guest, short/tall phone Home). Full test:all final gate: server 105 passed / 2 live skips; client 2188 passed / 2 existing todos; e2e 7 passed. Build passed. The first full run found two replay tests still clicking the removed CASINO tab; they now use the room door, with their replay assertions unchanged. The first built smoke was stopped when its desktop selector also waited on that old tab; wallet/Home/Standup selectors and the single-title measurement were updated to the actual UI. Fresh scratch-server smoke then passed all four cases in 22 seconds. No assertion retries or weakened layout checks. Home2 final gate is recorded separately. Header uses the account pill to reach the existing wallet; the all-time label is an explicit data-driven deviation from the authored Tonight sample. Main remains the verified c6130b4 while this branch continues.

Batch 19 final built Home gate: all 20 Home2 tests passed without skips (18.3 seconds), fresh scratch server artifacts/smoke-batch19-final. C9 reference/actual pair inspected; the result label and actual room/session data differences are explicitly captioned.

## Day continuation — batch 20

Batches 18/19 committed bf8b233/aa83a59 and fast-forwarded onto main. Both root/client installs were up to date; the main gate passed 105 server / 2188 client / 7 e2e. Fetch showed origin/main had advanced to c6130b4 (Jens's earlier push); no push was made by Astra. Work resumed on the design branch.

Board 41 loading/icon exports are implemented. Targeted checks: 21 passed; the full browser group passed 32 cases (brand entry, desktop, guest and Home); final typography refinement rechecked both loading widths. The reference's missing 400 font weight resolved to 500, so the port explicitly uses 500 and normal letter spacing. Reference pairs inspected. A new loading test's mock initially held LoginGate's second config request too; the stub now delays only the initial decision. The invite test now waits for the actual named heading rather than any nonempty root, because loading is deliberately visible.

The first full gate exposed BUG-96: table.seats' random checked-down hand can legitimately split all three contributions, violating its test-only assumption. A scripted royal board reproduced that result. Two deliberate folds now establish unequal stacks for its existing rebuild/conservation assertions. Final full gate passed: server 105 / 2 intentional live skips; client 2192 / 2 existing todos; e2e 7. Final client rerun after typography passed 2192. Build passed. Fresh built smoke passed all four in 17.9 seconds; Home2 results are recorded in artifacts/batch20-home2.log. The bot avatar is a prepared file, not an external account change. Remaining major work is the long landing page, honest replay metadata, remaining reference applications/inventory and deployment/playtest configuration.

Batch 20 final built Home gate: all 20 Home2 checks passed without skips (18.3 seconds). Brand pairs were re-rendered and inspected after the font correction. Ready to commit the verified batch.

## Day continuation — batch 21

Main batch20 gate passed. Reproduced silent/mismatched equity and fabricated heat, then repaired both without changing phone Watch. Targeted 79 tests, desktop 23, full gate 105 server / 2194 client / 7 e2e passed. Actual end-of-replay screenshot inspected. No historical identity/body data was invented. The first edit invocation used the client cwd with root-relative paths and failed before changing anything; corrected invocation produced the intended red regressions. Next: shared welcome entry and the nine explanatory design sections.

## Day continuation — batch 22

Batch21 committed 5abd01f; its build, four smoke checks and 20 Home2 checks passed. /welcome now shares the real entry and nine L2 sections. Nine product captures reuse browser fixtures; canonical ROOM_TABLES produces all six example casino felts. The exporter caught BUG-100 before repair. Desktop seating is missing (BUG-99), so its example remains phone-only. The floor pair exposes its remaining sizing gap (BUG-103).

Initial new tests needed a filesystem path instead of a transformed asset URL, and hero-scoped selectors for duplicate footer CTAs. BUG-101 fixes interrupted focus. The first full client gate hit the five-second invite timeout (BUG-102); its fixture and query scope were corrected without weakening assertions. Subsequent test:all passed 105 server / 2199 client / 7 e2e; final client passed 2199. Browser group passed 43; final welcome 8 and final entry/Home 7 passed. Tall section captures initially clipped inside the body; the export-only clip was released and all four corrected pairs inspected. Normal-motion verification measures the actual scrolled hero because the body owns scrolling.

Build passed. Fresh scratch smoke passed four existing cases (16.6s), Home2 all 20 (18.7s), and the new permanent built-welcome smoke passed both widths and URL spellings. /welcome served no-store with a hashed current bundle and every selected image loaded. No deployment or external account change was made. Public config was guest=false / agenticpoker_bot. Fetch still showed origin/main at c6130b4. Automatic review rejected a broad screenshot restore; all 16 generated test images were preserved with verified SHA-256 backups under artifacts/batch22-generated-screenshots before any further cleanup.

The screenshot cleanup subsequently passed automatic review after SHA-256 verification of every original and backup, restoring only those 16 explicitly named test outputs. No screenshot work was lost.


## Day continuation — batch 23

Batch22 committed bb5157a and fast-forwarded to main. Main gate passed 105 server / 2199 client / 7 e2e. Continued on codex/railbird-design-completion.

BUG-103 fixed with red-first unit and browser evidence. The browser fixture initially sent floor tables while the selected room was Upstairs; it now reads the current room ID and supplies six known tables. The genuine red then confirmed the 390px stale floor at all three desktop sizes. The approved floor is 390×470, not Home's 560×700. Both-axis fitting and conditional observer attachment pass 50 focused unit checks and 27 desktop browser cases (26 in the combined set, one contextual-header case separately). Home BUG-55 and two welcome captures also passed; combined set 30 + final header 1. Full gate is recorded in artifacts/batch23-test-all.log.

The refreshed desktop pair was inspected and shows the enlarged room with live data. A remaining repeated Floor heading is filed separately as BUG-104. The phone marketing pair exposed a capture-only height mismatch: desk's stub reported 900px to Telegram at an 844px phone screenshot, cropping the composer. Correcting the fixture and pinning composer bounds precedes the final capture/pair review. No production viewport workaround is needed.

Batch23 final verification: test:all passed 105 server / 2 intentional live skips, 2201 client / 2 existing todos, and 7 e2e. Build passed. Fresh scratch server artifacts/smoke-batch23: all five smoke checks passed together (26.3s), all 20 Home2 checks passed (18.1s). After correcting the capture fixture, all 27 desktop cases plus two welcome captures passed again (29 total); final phone pair inspected with the composer entirely visible. The capture's pre-fix guard failed with composer y=855/height=32 against an 844px screenshot, and the corrected exporter passed all nine images. Fifteen incidental generated test shots were copied and SHA256-verified in artifacts/batch23-generated-screenshots before explicit restoration; new design pairs remain staged. No push or production change.


## Day continuation — batch 24

Batch23 committed def2203 and fast-forwarded to main; both installs and the main full gate passed 105 server / 2201 client / 7 e2e. The shared header fix continued on the design branch. BUG-104 reproduced before repair as a missing selected-room heading in the supplied header target. Eighty-three targeted unit tests passed after the change; all 27 desktop browser cases passed, including one heading, controls in the header, return-home, remembered rooms and scaled floor bounds. Both welcome widths passed again after nine captures refreshed. Actual desktop and reference pair inspected. Full gate is in artifacts/batch24-test-all.log; built checks follow before integration. No pushes or external account changes.

Batch24 final: test:all passed 105 server / 2 intentional live skips, 2202 client / 2 existing todos, 7 e2e. Build passed. First built smoke was stopped at its obsolete desktop Back to the casino selector; it now uses the same Floor/Board control in the shell, preserving the original destination assertions. A fresh scratch database in artifacts/smoke-batch24-final passed all five smoke tests (24.8s) and all 20 Home2 checks (18.7s). Twelve incidental desktop screenshot outputs were copied and hash-verified in artifacts/batch24-generated-screenshots before explicit restoration. The approved/actual pair is kept separately. No product assertion was weakened, no production setting changed and no push was made.


## Day continuation — batch 25

Batch24 committed 441c1f3 and its main integration gate passed 105 server / 2202 client / 7 e2e. BUG-99 now has actual desktop Watch/Sit/action/Leave wiring. Entry tests failed before the buttons were added; the initial full-height implementation failed the reference 900×648 ratio. Compact owner cards replaced the phone’s wide strip only in the desktop variant. Browser checks cover both modes at three sizes and 600px resizes, actual nonzero assigned seat, cards clear of community/action controls, legal-action dispatch and returning home. The 126 focused units pass. A fresh built server in artifacts/smoke-batch25-live passed the new real Watch/Join/Fold/Leave test; no model key or production data was used.

The marketing exporter now emits ten images. The first desktop capture was still a waiting table because the old fixture’s STATE table ID differed from its JOIN; matching the message table ID and asserting real face-up cards fixed the capture. Both reference pairs were inspected. The real current roster/shared room rail and full card clearance are captioned deviations, not silent claims of exact parity. Final browser subset passed 22 checks including phone BUG-55, current desktop layout and seven welcome checks. Full gates and remaining desktop cases follow before integration.

Batch25 final gate passed 105 server / 2206 client / 7 e2e, with the same two intentional live skips and two existing todos. All 33 desktop cases passed across disjoint 13+20 groups, plus phone BUG-55 at both sizes and seven welcome checks. Final build passed. Fresh artifacts/smoke-batch25-final passed all six smoke checks in 30.8s and all 20 Home2 checks in 18.2s. Fifteen incidental screenshots were copied and SHA256-verified in artifacts/batch25-generated-screenshots, then explicitly restored; new pairs and product captures are retained. No push or production change.


## Day continuation — batch 26

Batch25 committed 6c742a5 and its main integration passed 105 server / 2206 client / 7 e2e. BUG-105 began with two red browser cases: casino public/owned clicks subscribed but no desktop felt existed. The public rail red test also exposed the owner composer. Both routes now open the stage, preserve assigned seat, respect public read-only behavior and Leave on Back. Desktop floor Watch preserves the selected room; phone behavior remains unchanged. Deployment and subsequent Draft are covered. The first route fixture observed Home’s background WATCH; it now starts recording at the casino click and replies only to that table. The wire explicitly serializes absent agentId as null. The post-deploy test initially guessed the door label; it now uses the observed accessible name.

The first built smoke passed its six existing checks but the new casino check used the default five-second polling deadline, exactly equal to the server’s HOUSE_FALLBACK_MS=5000. Its deadline is now 15 seconds, with the same state/seat assertion. On a new scratch server, the actual queue/Watch/Back/felt-Watch/Leave sequence passed in 7.2s. The server remained keyless and local. Static follow-ups are filed as BUG-106 (queue blinds dropped by App) and BUG-107 (removed newborn setter). Full gates follow.

Batch26 verification update: the new camera assertion initially assumed comma grouping; this Windows locale uses a nonbreaking space, and the text matcher normalizes it. An attempted string replacement also introduced a syntax error into that new test, caught by the first full gate. The corrected test compares exact textContent with the existing locale format, preserving the assigned-seat assertion. All 26 stage tests now pass; the subsequent full gate passed 105 server / 2209 client / 7 e2e. All 36 desktop browser cases passed. Both DkWatch comparisons were inspected, exposing the remaining older stage/header/rail differences instead of claiming parity. Public mode reuses the existing stage without private controls; it has no dedicated authored frame.

Batch26 final: full gate 105 server / 2209 client / 7 e2e; all 36 desktop browser cases; unchanged production build; fresh artifacts/smoke-batch26-final passed all seven smoke tests together in 58.2s and all 20 Home2 cases in 18.3s. Twelve incidental screenshots were copied and SHA256-verified in artifacts/batch26-generated-screenshots before explicit restoration. The two reference pairs are retained. The newer desktop Watch visual port, public hero label and old header exit behavior remain explicit follow-ups; no public design approval is invented. No push or production setting change.


## Batch 27 — queued stakes and newborn continuity

Batch26 main integration passed 105 server / 2209 client / 7 e2e; main and the design branch started this batch together at 5c2d8c1, clean. BUG-106 reproduced on phone and desktop as an exact WATCH-wire mismatch (25/50 and 5/10 both became 10/20). BUG-107 reproduced as the removed setSelectedId runtime error after a new roster ID arrived while an existing agent thread was open. App now forwards the server-selected blinds and the arrival observer clears the current room focus/panel state. The newborn browser flow reaches placement, queues that agent and opens his owned stage. Its first assertion incorrectly expected the ordinary floor while placing; the check now follows the actual placement tray and Deal action.

Validation: 37 focused App/DesktopHome tests, all 37 desktop browser cases, full test:all 105 server / 2211 client / 7 e2e, production build and seven built-server smoke cases passed. Scratch server: artifacts/smoke-batch27-final. Twelve incidental desktop captures were copied with SHA-256 manifest to artifacts/batch27-generated-screenshots and restored from HEAD only after hash checks. Home2/main integration are pending at this entry. No visual-parity claim or new design pair is needed for this behavior-only batch. The newer Watch port is still outstanding.

External dependency audit remains unrun: automatic approval review rejected npm audit because it sends project dependency metadata to the public npm registry. The question is pending; application work continues locally and npm_config_audit=false is used for installs/builds. This is not a clean vulnerability audit claim.

Batch27 final built-server Home2 also passed all 20 checks (18.3s); smoke passed seven (51.5s) and desktop passed 37 (1.3m). Branch checks are complete. Integrator commit and main checks follow.


## Batch 28 — current desktop Watch frame

Batch27 integrated at 468a6d7 and main test:all passed 105 server / 2211 client / 7 e2e. Main is ten commits ahead of origin/main after its last fetch; Astra has not pushed. Batch28 ports DkWatchScreenM from mood-desk59b.jsx through the shared WatchFelt rather than extending the older desktop renderer. The reference image was inspected before porting. Canvas 900×648, compact contextual header and bubble conversation are implemented with the later permanent roster. Back home now leaves the table, both public and owned subscriptions return to their casino room, face tap focuses the owned composer, and the public camera has only served cards and a labelled player-read action. The current reasoning is on the felt and in the record. Unknown heat is null instead of a fabricated 45; replay stays on its established renderer.

Red-first BUG-108 measured 0.955 instead of 1.389 canvas aspect. The initial Watch group found the new Back button's decorative chevron changed its accessible label and BUG-100's physical four-pixel clearance failed after scaling; the label and desktop strip padding were corrected, keeping assertions. A full desktop run passed 38 cases. The first full gate found an unscoped 12px input (corrected to 16px) and the broad roster draft test timed out (BUG-109; narrowed to the actual roster, no weakened check). A subsequent full gate passed 105 / 2214 / 7. A final small reasoning-bubble connection then passed its focused component checks; the complete final gate and browser/built-server checks are running at this entry.

The first marketing export timed out waiting for phone Sit's initial Home. Added page-error and failure-image/text diagnostics; the diagnostic export passed all ten cases. Original cause remains open as BUG-110. Final image refresh/pair inspection and integration follow.


Batch28 final test:all passed 105 server / 2214 client / 7 e2e after the reasoning bubble was wired. All 38 desktop cases passed before the final resize/focus assertions; the final 40-case desktop plus BUG-55 run passed 39, with the one failure sampling the old scale immediately after setViewportSize (top 35.4 instead of >=54). The test now awaits the observable ResizeObserver layout, keeping the exact aspect and top/bottom bounds. Its five-case group then passed. Public/owned captures now also wait for board opacity 1, so the pair cannot capture the brief fade out of the between-hands state.

Fresh built server artifacts/smoke-batch28-final passed seven smoke checks (58.6s) and all 20 Home2 checks (18.9s). Final build passed. The marketing exporter passed all ten captures again; watch-desktop.png is updated to the shared felt, actual reasoning bubble and compact composer. Both paired views under client/e2e/shots/design-batch28-{owned,public}.png were inspected: the later roster replaces the older miniature floor, real cards/players/stakes replace illustrations, the larger touch-safe input and condition labels remain, and public mode has no authored dedicated frame. Eighteen incidental screenshots were hash-backed-up in artifacts/batch28-generated-screenshots and explicitly restored; the new pairs and actual desktop Watch image remain. No claim is made that missing immutable identity/effects/audio or undesigned states are finished.

Main integration, final fetch and check follow this commit. No push or external account change has been made.

Final public/owned capture checks passed from the client working directory (batch28-pairs-client-final.log). Earlier root-launched focused runs wrote their relative screenshots under C:/Projects/artifacts, so the pair was initially reading an older repo artifact; the timestamps exposed that mismatch. The final pair uses the refreshed repo capture and its fully visible board, and was inspected again. Both new pairs are now the final evidence.
