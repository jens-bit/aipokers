# Railbird — 10 September continuation

## Checkpoint 01:28 Europe/Malta — batch46 final integration

No push, deploy or VPS change. The integration branch combines settlement `df873317` (including `9fede1d`), refused Watch feedback `c7d7e43`, actual Home profile/live scope `53a02a1`, and first-paint game knowledge `47573fd` with the earlier batch46 work. All final gates are now green; the integration is ready for a local fast-forward from main `e338a97`. Origin was fetched successfully; no new remote changes were reported.

The frozen combined source passed **121 server checks, 2,381 client tests in 181 files, and 7 gameplay checks**, with two intentional server skips and two existing client todos. Fresh built smoke passed 8 and Home layout passed 20. The full desktop run passed 51 and exposed two old BUG131 assertions expecting practice winnings as money. The correction was made in the owning Home-profile worktree, then copied into integration: both widths now require the actual $2,000 pocket and unknown casino result while retaining live marker, visiting label, stakes and Watch assertions. Both corrected cases passed, then the complete desktop gate passed all 53. The original failing log is preserved. No product source changed after the full regression gates or during the playtests.

The real signed three-owner invitation journey now passes all seven stages, including each owner's actual visible cards, working Profile Watch/LIVE, unchanged pockets, third-owner refusal, expiry return and expired invitation feedback. Desktop refusal also passes with a 44px Back home target. Native share cancellation/clipboard are locally substituted; no Telegram message was sent. The main bundle is `index-ThTyOPpJ.js`, SHA256 `269f9de7805434215249cd5162646c108fcc63a0033ae2a4575836d065a95853`.

Two further full 120-second observations completed on this exact product source. Normal Home hand gaps were 3.007–3.027 seconds; no interaction/network errors or requests over 300 ms were recorded. The remaining stale Home conversation is BUG163 and has a separate isolated repair under review. F05's solo-TV trigger is an explicit design decision, not a reason to override the fresh Rock's real reading behavior. The neutral name-pill correction is next. [Playtest report](PLAYTEST_2026-09-10.md) retains both observations and their limits.

Design counts remain **0 SHIPPED, 99 PARTIAL, 80 BUILT?, 1 NOT BUILT, 0 NOT DESIGNED, 5 SUPERSEDED**, covering 185 frame/inset rows, plus 466 substates and five unboarded interactions. No new full-frame match is claimed. New named defects are BUG163–166; the BUGS header is recounted at 158, including the historical duplicate121. The source and browser test contract changes are local and reviewable; the overall objective remains active.

## Checkpoint00:45 Europe/Malta

Work remains active; no push, deploy or VPS setting change. Local main remains e338a97. The integration branch now contains the original three repairs below, Home capacity/loading60bfbd8, consent/membership/sharingc1815f3, visiting-profile Watch95a57e3, and preview86abacc. The combined preview gate passed120 server,2358 client and7 gameplay checks; fresh built smoke passed8. Remaining Home/desktop gates and follow-up fixes are still in progress.

The initial consent/share combination passed120 server,2351 client and7 gameplay checks. BUG160 then passed120 server and2353 client; its74 focused checks cover real navigation intent and the owner's current kitchen viewpoint. Preview independently passed118 server,2337 client and10 phone/desktop browser cases. Its full gate rediscovered BUG37 from its older branch base; the identical already-tested formatter fix was included, preserving the failing ceremony assertion.

Actual built three-owner journey passed Share/cancel/Copy, token preview, pending knock, refusal during a host hand and admission at the legal boundary. It exposed the visiting-profile Watch dead button (BUG160, now repaired), then a cold private-table link correctly denied on wire while the UI concealed the refusal (BUG161, isolated repair underway). The wager review independently played39 and40 real synthetic hands and exposed loss of the closing result before the40-hand sweep (BUG151 follow-up, isolated repair underway). No complete invitation journey or final playtest is claimed yet.

The rebuilt journey then passed the sender's actual Watch/card view. It exposed a missing resident-Home live profile and incorrect RESTING Watch tag (BUG162). That repair is isolated from the refusal and settlement work. The second journey's failure is recorded in batch46-visit-journey-after160.log; it is not described as a complete pass.

Root and independent visual reviewer inspected the preview component pairs. The felt now reads the existing public board and saved identities at358×92px on the phone; paced3→4→5 and one watcher are verified. Supplemental header, portrait/card treatment, actual occupants and desktop rail remain explicit differences. The desktop evidence is a native phone component reference beside the actual desktop, not a board31 frame match. All inventory counts remain unchanged. The new two120-second observations are prepared and will run after the final combined build.

## Earlier checkpoint

00:12 Europe/Malta. Work remains active. Nothing has been pushed or deployed.

Local main remains batch45 at e338a97. The integration branch now adds:

- 5a023ff: BUG37 result deltas use the established wallet format. A Swedish-locale regression reproduces the original mismatch; unrelated old desktop/floor formatting remains open.
- c31b2b5: BUG159 corrects the latent BirthScreen edit-mode target field. Current App opens BirthScreen only for creation, so no new reachable edit entry is claimed.
- 7ceab7c: BUG158 zero-career Home players use eight seated nature greetings when the live kitchen proves their seat. Existing session recaps and private voice remain intact.

Root source gates:118 server checks passed with2 intentional skips;2323 client tests passed with2 existing todos;7 gameplay checks passed. Fresh client build passed. The first broad run exposed BUG37's actual locale mismatch; its exact result test was retained and the product formatter fixed. Logs are artifacts/batch46-root-full-final.log and batch46-root-build.log.

The built Home regression at390×844 used synthetic local data and normal pacing. Wild Card remained seated, requested beer once, and said “This hand first. Then we make something happen.” The real thread expanded and closed correctly. Screenshot and structured record: artifacts/bug158-home-seated.png and bug158-built-browser.json. This focused journey is not a substitute for the next two full two-minute playtests.

Parallel work: Home capacity/loading fixes passed focused and browser checks and are entering their full gate. Owner-issued visit consent, durable return/escrow, private-kitchen membership, readable share feedback, and the guest/bot invitation bridge are still being integrated in isolated branches. Claiming a guest while hosting a visitor and actual human-seat capacity are included in the lifecycle review.

Visual inventory unchanged:185 authored frames/insets plus466 substates and5 unboarded interactions. Frame counts remain0 SHIPPED,99 PARTIAL,80 BUILT?,1 NOT BUILT,0 NOT DESIGNED,5 SUPERSEDED. Existing complete-screen mismatches remain explicit. Next: integrate coherent branches, verify the real two-owner invitation journey, run fresh built gates and playtests, then continue current reference ports.
