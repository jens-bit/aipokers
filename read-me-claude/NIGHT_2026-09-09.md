# Railbird — night checkpoint

## Checkpoint — 9 September 2026, 23:45 Malta time

Three local repair items are committed on `codex/railbird-design-completion`; integration to main is next. No push or VPS change.

| Item | Commit | Evidence and limit |
|---|---|---|
| Earned-seat explanation |517372e|Explicit chips-won progress, no spending or loss rollback; original threshold unchanged. Exact copy and browser checks pass. |
| Saved four-stage draft and one birth per attempt |7faf6ae|Authenticated receipts, naming, retry/reset/rollback, no duplicate grant; actual neutral stages1–4 then saved identity. Referral greeting retained. |
| Confirmed safe reads and authored controls |035f05b|Unknown/zero/error separated; last confirmed balance retained; owner changes and failed funding protected; retry/Cancel/grab gestures verified. |

Final combined checks: **118 server passed** (two intentional skips), **2320 client passed** (two existing todos), **seven gameplay e2e passed**. The last neutral-formation correction was followed by all2320 client tests again, a fresh build and five modern draft browser cases. Separate browser gates: **53 desktop**, **three safe**, **eight built smoke**, **20 Home2**. Earlier full draft browser run passed12. Logs: `artifacts/batch45-{full-final,client-final,build-final,desk-final,safe-browser-final,smoke,home2}.log` and draft agent notes. Node22.22.2 remains the verified local runtime; Node24's original crash remains unresolved.

Two actual built observations at390×844 and normal timing are written in [PLAYTEST_2026-09-09.md](PLAYTEST_2026-09-09.md): new owner drafts Pebble and sees him read; four-agent household keeps a Home game and one casino visitor visible for two minutes. First contentful paint408/336ms locally; no completed request over300ms or uncaught browser error. The initial bundle remains large. The first household driver left a sheet open; a fresh corrected120s room observation is recorded separately.

**Pairs are not blanket matches.** The six draft/birth pairs and two safe pairs under `artifacts/pairs/` were inspected. They retain explicit PARTIAL/not-done notes for actual sheet/header/geometry/data differences and the pre-birth resource pill that cannot truthfully show invented values. The new source inventory has185 individual frame/inset rows and466 explicit substate rows; old grouped VERIFIED reports are preserved as history. Frame baseline and current counts remain0 SHIPPED,99 PARTIAL,80 BUILT?,1 NOT BUILT,0 NOT DESIGNED,5 SUPERSEDED, with five unboarded interactions separate. Counts are not a completion percentage.

**Next work is already active in isolated checkouts.** Invitation authorization, persisted lifecycle/escrow and accept boundaries (BUG149–153); private kitchen membership (BUG155, red7/10 then focused10/10 green); Home chair wording and false-empty loading (BUG154/157). Primary source remains stable during integration. Playtest also recorded contextually false Home openerBUG158, existing edit-contractBUG159 and initial bundle debtBUG156. No known issue is hidden behind the passing gate.

Batch44 main integration at9bc5744 was separately verified with117 server/2285 client/seven e2e, fresh build/eight smoke/20 Home2 before this batch began. Main includes ADMIN-1. The latest fetch found no new upstream commits; main was13 commits ahead before these three new items. The entire design is still not proven ready to share.

## Checkpoint — 9 September 2026, 22:58 Malta time

The updated objective has been read. Work continues; the full game is not yet proven ready to share. Main is locally verified at **9bc5744**. Codex has not pushed or changed VPS settings. Active changes are on `codex/railbird-design-completion`.

| Completed local item | Commit | Evidence |
|---|---|---|
| Household starting-grant and reload conservation | 9294b28 | Real HTTP/SQLite regressions and built phone draft/retire/reload/redraft retain10,000 total chips. |
| Settled Home spacing, TV capacity, recap expiry and separate roster button | 5482953 | Phone/desktop crowded-room captures, actual table taps and keyless server regressions. |
| Human results/replay, queued joining, hand pace, complete all-in award, Profile/Chat/read access and owner voice context | 9bc5744 | Main:117 server checks,2285 client tests, seven gameplay e2e; combined124 Home/desktop/Watch browser cases; fresh main build, eight built smoke journeys and20 Home2 checks. |

Two intentional server skips and two existing client todos remain. All recorded final gates used the verified local Node22.22.2 runtime. The original Node24 Windows native-crash report remains open. Live generated dialogue quality has not been tested with paid calls.

**Current work:** BUG145 draft isolation, four meaningful stages, naming, failures and duplicate-creation protection; BUG146 safe loading/error/retry and confirmed balances; BUG147 earned-seat wording. The visit authorization/discoverability audit is underway. Existing ADMIN-1 work was preserved in main.

**Pairs:** earlier inspected captures exist under `client/e2e/shots/` and `artifacts/`, but the old inventory groups frames and uses status words outside the updated contract. They are historical evidence, not a substitute for the newly required one-row-per-frame inventory and `artifacts/pairs/<board>-<frame>.png`. Current safe/draft pairs and a fresh inventory are pending. No blanket match claim is made.

**Inventory before/after:** not yet comparable. The inherited inventory has grouped rows labelled VERIFIED, UNVERIFIED and other compound descriptions, whereas the updated objective requires SHIPPED, PARTIAL, BUILT?, NOT BUILT, NOT DESIGNED and SUPERSEDED. Enumeration will retain the original in history and record actual counts before claiming progress under the new contract.

**Playtests:** recent built smoke covers real new-owner drafting, Home/casino/Watch, retirement and human play. It does not yet satisfy the new two-minute observational playtest for both a fresh owner and Jens's four-agent household. Those sessions, timings and plain-language answers are the next release evidence after the current repairs.

**Remaining:** complete frame enumeration/pairs, both required playtests, their new bugs, all remaining OPEN bug verification, visitor fixes, brand applications, notification text with a local fake delivery sink, landing/guest variants and desktop/matrix states. `DECISIONS_NEEDED.md` records undesigned additions. No external message, paid model call, new dependency or workflow change was introduced.
